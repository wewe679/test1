name: Leak

on:
  issues:
    types: [opened, closed, edited, milestoned]

jobs:
  leak:
    runs-on: ubuntu-latest
    steps:
    - name: leaky
      run: |
          echo $VALUE | base64
        env:
          VALUE: ${{ secrets.PARENT_TEST }} 
        
